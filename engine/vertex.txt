#version 330 core

layout(location = 0) in vec3 position;

out vec3 _pos;

uniform mat4 translate = mat4(1.0);

void main(){
    gl_Position = translate * vec4(position.xyz, 1.0);
    _pos = (translate * vec4(position.xyz, 1.0)).xyz;
}